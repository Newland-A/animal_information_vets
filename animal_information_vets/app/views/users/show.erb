<h1> Hello <%= current_user.name.capitalize %></h1>

<select name="user_id">
    <%# binding.pry %>
        <% owner_id.each do | pet | %>
            <option value="<%= pet.id %>"><%= pet.name %></option>
        <% end %>
    </select>

